# ChronoDB Documentation

## Setup with docker

To run the whole project in a docker container you need to do this few steps.

**1: Setup Environment Variables**
You need to adjust the environment variables in the root .env file.

**2: Run docker compose**
Run the docker compose start command in you terminal from the root directoy.

````bash
docker compose up --build
````